apiVersion: v1
kind: Service
metadata:
  name: testsvc
  labels:
    feladat.banzaicloud.io/ingress: secure
    cica: mica23
spec:
  selector:
    app: MyApp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376